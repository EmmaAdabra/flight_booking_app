package utils;

import ch.qos.logback.classic.LoggerContext;
import ch.qos.logback.core.Appender;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class LoggingUtil {
    public static void disableConsoleAppender(){
        // Get the LoggerContext from Logback
//        LoggerContext loggerContext = (LoggerContext) LoggerFactory.getILoggerFactory();

        // Cast the root logger to Logback's Logger class
//        Logger rootLogger = loggerContext.getLogger(Logger.ROOT_LOGGER_NAME);

        // Find the ConsoleAppender
//        Appender consoleAppender = rootLogger.getAppender("CONSOLE");

//        rootLogger.
    }
}
